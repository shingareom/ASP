Below is a complete implementation of a bootstrap loader for the 8086 object file format generated by your two‑pass assembler.
It reads output8086.obj, extracts segment base addresses from the header comments, loads the data and code bytes into a simulated 8086 memory array, and displays the loaded memory map along with the entry point.


./bootstrap_loader output8086.obj
